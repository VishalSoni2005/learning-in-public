#include <iostream>
#include <string>
using namespace std;
int main(){
    // char str[] = "vishal kumar soni"; //it is not 17 char   IT HAS 18 CHARACTER(one is null character)
    // cout<<str;
    string m;
    getline(cin,m);
    cout<<m;
}