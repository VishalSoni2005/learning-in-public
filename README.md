This branch contains the code of my genAI learnings

uv commands: 
1:  npm run dev (to start the app) → uv run
2: npm install (to install dependencies) → uv sync
3: npm list (to list dependencies) → uv list
4: npm run build (to build the app) → uv build
5: npm test (to run tests) → uv test
