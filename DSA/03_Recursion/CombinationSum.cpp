#include <iostream>
#include <vector>
using namespace std;
void combinationSum(vector<int> &arr, int size, int target, int index) {
  
}

int main()
{
  vector<int>arr = {2, 3, 5};
  int size = arr.size();

  int target = 8;
  combinationSum(arr, size, target, 0);
  return 0;
}