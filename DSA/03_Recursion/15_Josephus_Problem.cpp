#include <bits/stdc++.h>
using namespace std;

void predict_the_winner(int n, int k, vector<bool> &track) {

}

int main() {
  int members = 5;
  int steps = 3;

  vector<bool> track(members, 0);

  predict_the_winner(members, steps, track);
}