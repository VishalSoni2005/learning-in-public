# Day 1 : concepts learned

-> compiled vs interpreted language
-> string decleration : char \*str
-> signed and unsigned datatype -> 1 byte (unsigned) = 0 to 255 and unsigned 1 byte = -256 to 256
-> size_t
-> Linkers => gcc my_math.c main.c -o math_program

# Day 2 : concepts learned
